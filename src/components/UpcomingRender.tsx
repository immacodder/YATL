interface P {}
export function Upcoming(p: P) {
	// } else if (p.currentProject.name === "Upcoming") {
	// 	defaultProjectFilteredTodos = p.todos.filter(
	// 		(todo) =>
	// 			todo.scheduledAt &&
	// 			isAfter(todo.scheduledAt, endOfDay(new Date()))
	// 	)
	// }
	return null
}
